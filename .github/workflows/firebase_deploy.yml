name: Firebase CD

on:
  push:
    branches:
      - master

jobs:
  firebase-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@master
        with:
          node-version: '14.x'
      - run: npm install
      - run: npm run build
      - uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting
        env:
          FIREBASE_TOKEN: ${{0gFODzcqtguJLCgYIARAAGBASNwF-L9Irgvk0uqK6aqUdMWYCUk73yLaBgQKz4ep2jzqhse2FiBuMmQgU5IJEXYWvk4kB7oqjXOk}}
